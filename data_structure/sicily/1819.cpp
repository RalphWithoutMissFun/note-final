#include <bitset>
#include <string>
#include <iostream>
#include <vector>
using namespace std;

void process(int n)
{
   vector<vector<int> >orig1 , orig2 , answer , correct;
   for (int index = 0 ; index < n ; index++)
}
int main()
{
  int test_case;
  cin >> test_case;
  while (test_case--)
  {
    int n;
    cin >> n;
    process(n);
  }
}
